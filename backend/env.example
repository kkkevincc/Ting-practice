# ========== 音频转写API配置 ==========
# SiliconFlow API密钥（用于音频转文字）
# 可选配置：未配置时系统将使用模拟数据
# 获取API密钥：https://siliconflow.cn/
SILICONFLOW_API_KEY=your_siliconflow_api_key_here

# ========== AI关键词筛选API配置 ==========
# AI API基础URL（用于智能关键词筛选，区别于音频转写）
# 默认使用 agentrouter.org
AI_API_BASE_URL=https://agentrouter.org/v1/chat/completions

# AI API密钥（用于智能关键词筛选）
# 注意：这是独立的API密钥，与音频转写API密钥不同
AI_API_KEY=your_ai_api_key_here

# AI API模型（用于智能关键词筛选）
# 默认使用 glm-4.6
AI_API_MODEL=glm-4.6

# 是否使用AI智能关键词筛选
# 设置为 true 时，系统会使用AI API分析转写文本和题目，智能选择与听力解答最相关的关键词
# 设置为 false 或未设置时，使用本地基于词频的关键词提取方法
# 注意：启用此功能需要配置 AI_API_KEY
USE_AI_KEYWORD_FILTER=false

# JWT密钥（用于用户认证）
# 生产环境务必修改为随机强密钥！
# 可以使用: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-key-change-in-production

# 服务器端口
# 后端API服务器监听端口，默认3001
# 前端开发服务器运行在3000端口
PORT=3001